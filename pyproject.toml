[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "argot-cli"
version = "0.1.0"
authors = [
    { name = "Lucas L. S. Haine", email = "lucaslshaine@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []
